sum(a: int):int {
    x: int, y: int = 1, 0;
    while (x <= a) {
      y = y + x;
      x = x + 1;
    }
    
    return y;
}

main() {
    x:int = sum(500);
    y:int = 501 * 250;

    assert(x == y);
}

