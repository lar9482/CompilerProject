sum(a: int):int {
    x: int, y: int = 1, 0;
    while (x <= a) {
      y = y + x;
      x = x + 1;
    }
    
    return y;
}

main() {
    x:int = sum(500000000);
    y:int = 500000001 * 250000000;

    println(x);
    println(y);
}

