main(): int {
    // x = 4
    x: int = 1 + 1 + 1 + 1; 

    // y, z = 20, 1
    y: int, z: int = 10 * 2, (5 / 5);

    // a = 5
    a: int = 55 % 10;

    // b = 0
    b: int = 5 - 5;

    // c = 10
    c: int = -10 * -1;

    // 40
    return a + b + c + x + y + z;
}